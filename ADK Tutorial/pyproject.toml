[project]
name = "adk-tutorial"
version = "0.1.0"
description = "A hands-on introduction to Google Agent Development Kit (ADK)"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "google-adk>=2.0.0",
    "python-dotenv",
    "pydantic",
    "jupyter",
    "ipykernel",
    "uvicorn>=0.52.4",
]

[tool.uv]
package = false
